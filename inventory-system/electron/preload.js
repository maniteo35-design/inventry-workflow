// Intentionally minimal: the renderer (the Next.js app) talks to the local
// backend over plain HTTP (http://127.0.0.1:5051/api), so no privileged
// Node APIs need to be exposed here. Kept as a placeholder / extension point
// (e.g. native file save dialogs for exports) if you need it later.
