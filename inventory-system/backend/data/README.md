This folder holds `inventory.sqlite` when running the backend with `DB_DIALECT=sqlite`
(the default for the offline desktop app). The actual `.sqlite` file is created
automatically on first run and is gitignored — don't commit it.
